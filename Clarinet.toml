[project]
name = 'Scholarix'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.academic-registry]
path = 'contracts/academic-registry.clar'
clarity_version = 3
epoch = '3.2'

[contracts.scholarship-lottery]
path = 'contracts/scholarship-lottery.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
